using System.Collections.Generic;

namespace ObserverPattern
{
    // this class must be generic
    public class Observable
    {
        // here you must create your subject (a private backing field)
        
        // here you must create a property that exposes the subject 
        // and modifies it (getter + setter)

        // here you must create a private list of observers

        // a constructor must initialize your subject 

        // create a subscribe method to add an observer

        // create an unsubscribe method

        // The notify observer notifies the observers when the
        // subject value changes
    }
}